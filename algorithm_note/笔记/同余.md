## 同余
```
1、模数、余数相同，则两个值相减模数后余数为0 (分开模相等 = 相减后模的值为0)
  a % p = z，(a - b) % p = 0 ==> b % p = z

2、模后的结果不为0， 判断 a 和 b 的关系
 (a - b) % p = z ==> b % p = (a - z) % p

3、相加同余 （和的模 = 分开模的和的模）
(a + b) % p ==> (a % p + b % p) % p

4、乘法的余的 先余再乘后余
a * b % p ==> (a % p * b ) % p
```

